services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    restart: unless-stopped
    # Removed: ports - Access via Traefik at https://${TAILSCALE_HOST}:8096
    # Note: DLNA (1900/udp) and auto-discovery (7359/udp) disabled - configure clients manually
    volumes:
      - ./config:/config
      - ./cache:/cache
      - ${MOVIES_PATH:-/mnt/external/movies}:/media/movies:ro
      - ${TV_PATH:-/mnt/external/tv}:/media/tv:ro
      - ${MUSIC_PATH:-/mnt/external/music}:/media/music:ro
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
      - JELLYFIN_PublishedServerUrl=https://${TAILSCALE_HOST}:8096
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M
    networks:
      - media-network      # Keep for backward compatibility (Iteration 1)
      - frontend-net      # User-facing service
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8096/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "autoheal=true"
      - "traefik.enable=true"
      - "traefik.http.routers.jellyfin-public.rule=Host(`${PUBLIC_JELLYFIN_DOMAIN:-jellyfin.otterammo.xyz}`) || Host(`${JELLYFIN_DOMAIN:-jellyfin.lan}`)"
      - "traefik.http.routers.jellyfin-public.entrypoints=web"
      - "traefik.http.routers.jellyfin-public.service=jellyfin"
      - "traefik.http.routers.jellyfin.rule=Host(`${TAILSCALE_HOST}`)"
      - "traefik.http.routers.jellyfin.entrypoints=jellyfin"
      - "traefik.http.routers.jellyfin.tls=true"
      - "traefik.http.services.jellyfin.loadbalancer.server.port=8096"
