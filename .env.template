# Media Stack Environment Configuration
# Copy this file to .env and adjust values as needed

# User and Group IDs (use 'id' command to find your values)
PUID=1000
PGID=1000

# Timezone (use 'timedatectl' to find your timezone)
TZ=UTC

# Network name (change if you want a different network name)
NETWORK_NAME=media-network

# Data paths (relative to the media directory)
MOVIES_PATH=./data/movies
TV_PATH=./data/tv
MUSIC_PATH=./data/music
DOWNLOADS_PATH=./data/downloads

# External access (for Traefik or other reverse proxies)
DOMAIN_SUFFIX=.local
JELLYFIN_DOMAIN=jellyfin${DOMAIN_SUFFIX}
JELLYSEERR_DOMAIN=jellyseerr${DOMAIN_SUFFIX}
FILEBROWSER_DOMAIN=filebrowser${DOMAIN_SUFFIX}
SONARR_DOMAIN=sonarr${DOMAIN_SUFFIX}
RADARR_DOMAIN=radarr${DOMAIN_SUFFIX}
PROWLARR_DOMAIN=prowlarr${DOMAIN_SUFFIX}
BAZARR_DOMAIN=bazarr${DOMAIN_SUFFIX}
QBITTORRENT_DOMAIN=qbittorrent${DOMAIN_SUFFIX}

# Cloudflare Tunnel Configuration
# Get this token from your Cloudflare Zero Trust dashboard
# Visit: https://one.dash.cloudflare.com/ -> Access -> Tunnels
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here

# VPN Configuration (for qBittorrent protection via Gluetun)
# VPN Provider and Type
VPN_PROVIDER=mullvad
VPN_TYPE=wireguard

# Mullvad WireGuard Configuration
# Get these from: https://mullvad.net/en/account/wireguard-config
# Generate a key and copy the private key and address
WIREGUARD_PRIVATE_KEY='your_wireguard_private_key_here'
WIREGUARD_ADDRESSES=10.x.x.x/32

# VPN Server Selection
# Choose a city close to you for best performance
# Examples: Chicago, Los Angeles, London, Tokyo
VPN_SERVER_CITIES=Chicago

# Port Forwarding (optional, improves seeding)
# Request a port from Mullvad account, then set it here
VPN_INPUT_PORTS=6881
FIREWALL_INPUT_PORTS=8080

# Firewall Configuration
# Docker network subnet (check with: docker network inspect media-network)
# and your local LAN subnet
FIREWALL_OUTBOUND_SUBNETS=172.18.0.0/16,192.168.1.0/24

# Fail2ban Configuration (optional)
# Email notifications for banned IPs
FAIL2BAN_EMAIL=
FAIL2BAN_SENDER=fail2ban@localhost
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_TLS=YES
