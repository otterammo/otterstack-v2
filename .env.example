# Media Stack Environment Configuration
# Copy this file to .env and adjust values as needed

# User and Group IDs (use 'id' command to find your values)
PUID=1000
PGID=1000

# Timezone (use 'timedatectl' to find your timezone)
TZ=UTC

# Data paths
MOVIES_PATH=/mnt/external/movies
TV_PATH=/mnt/external/tv
MUSIC_PATH=/mnt/external/music
DOWNLOADS_PATH=/mnt/external/downloads

# External access (for Traefik)
# - TAILSCALE_HOST is used for admin entrypoints (Authelia-protected)
# - PUBLIC_*_DOMAIN are used for Cloudflare/public routes
# - SERVER_IP is used for LAN display links in the Web UI
TAILSCALE_HOST=your-hostname.ts.net
SERVER_IP=192.168.1.100
PUBLIC_JELLYFIN_DOMAIN=jellyfin.your-domain.com
PUBLIC_JELLYSEERR_DOMAIN=jellyseerr.your-domain.com
PUBLIC_DASHBOARD_DOMAIN=dashboard.your-domain.com

# VPN Configuration (for qBittorrent protection via Gluetun)
VPN_PROVIDER=mullvad
VPN_TYPE=wireguard

# Mullvad WireGuard Configuration
# Get these from: https://mullvad.net/en/account/wireguard-config
# Store the PrivateKey in secrets/wireguard_private_key (single line, no quotes)
WIREGUARD_ADDRESSES=10.x.x.x/32

# VPN Server Selection
VPN_SERVER_CITIES=Chicago

# Port Forwarding (optional)
VPN_INPUT_PORTS=6881
FIREWALL_INPUT_PORTS=8080

# Firewall Configuration
FIREWALL_OUTBOUND_SUBNETS=172.20.0.0/16,192.168.1.0/24

# =============================================================================
# SECRETS
# =============================================================================
# Create these files in the secrets/ directory:
#
# secrets/cloudflare_token       - Cloudflare Tunnel token
#                                  Get from: https://one.dash.cloudflare.com/
#                                  -> Access -> Tunnels
#
# secrets/grafana_admin_password - Grafana admin password
# 
# secrets/webui_admin_password   - Web UI admin password
#
# secrets/wireguard_private_key  - Mullvad WireGuard PrivateKey (single line)
#
# Set permissions:
#   chmod 700 secrets/
#   chmod 600 secrets/*
