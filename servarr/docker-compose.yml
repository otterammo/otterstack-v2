services:
  sonarr:
    image: linuxserver/sonarr:latest
    container_name: sonarr
    restart: unless-stopped
    # Removed: ports - Access via Traefik at http://sonarr.lan
    volumes:
      - ./sonarr/config:/config
      - ${TV_PATH:-/mnt/external/tv}:/tv
      - ${DOWNLOADS_PATH:-/mnt/external/downloads}:/downloads
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 256M
    networks:
      - media-network      # Keep for backward compatibility (Iteration 1)
      - backend-net       # Business logic layer
      - download-net      # Connect to qBittorrent
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8989/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "traefik.enable=true"
      # HTTPS router
      - "traefik.http.routers.sonarr.rule=Host(`${SONARR_DOMAIN:-sonarr.lan}`)"
      - "traefik.http.routers.sonarr.entrypoints=websecure"
      - "traefik.http.routers.sonarr.tls=true"
      - "traefik.http.routers.sonarr.service=sonarr"
      # HTTP redirect router
      - "traefik.http.routers.sonarr-http.rule=Host(`${SONARR_DOMAIN:-sonarr.lan}`)"
      - "traefik.http.routers.sonarr-http.entrypoints=web"
      - "traefik.http.routers.sonarr-http.middlewares=redirect-to-https"
      - "traefik.http.routers.sonarr-http.service=sonarr"
      # Service definition
      - "traefik.http.services.sonarr.loadbalancer.server.port=8989"

  radarr:
    image: linuxserver/radarr:latest
    container_name: radarr
    restart: unless-stopped
    # Removed: ports - Access via Traefik at http://radarr.lan
    volumes:
      - ./radarr/config:/config
      - ${MOVIES_PATH:-/mnt/external/movies}:/movies
      - ${DOWNLOADS_PATH:-/mnt/external/downloads}:/downloads
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M
    networks:
      - media-network      # Keep for backward compatibility (Iteration 1)
      - backend-net       # Business logic layer
      - download-net      # Connect to qBittorrent
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7878/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "traefik.enable=true"
      # HTTPS router
      - "traefik.http.routers.radarr.rule=Host(`${RADARR_DOMAIN:-radarr.lan}`)"
      - "traefik.http.routers.radarr.entrypoints=websecure"
      - "traefik.http.routers.radarr.tls=true"
      - "traefik.http.routers.radarr.service=radarr"
      # HTTP redirect router
      - "traefik.http.routers.radarr-http.rule=Host(`${RADARR_DOMAIN:-radarr.lan}`)"
      - "traefik.http.routers.radarr-http.entrypoints=web"
      - "traefik.http.routers.radarr-http.middlewares=redirect-to-https"
      - "traefik.http.routers.radarr-http.service=radarr"
      # Service definition
      - "traefik.http.services.radarr.loadbalancer.server.port=7878"

  prowlarr:
    image: linuxserver/prowlarr:latest
    container_name: prowlarr
    restart: unless-stopped
    # Removed: ports - Access via Traefik at http://prowlarr.lan
    volumes:
      - ./prowlarr/config:/config
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M
    networks:
      - media-network      # Keep for backward compatibility (Iteration 1)
      - backend-net       # Business logic layer
      - download-net      # Connect to qBittorrent
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9696/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "traefik.enable=true"
      # HTTPS router
      - "traefik.http.routers.prowlarr.rule=Host(`${PROWLARR_DOMAIN:-prowlarr.lan}`)"
      - "traefik.http.routers.prowlarr.entrypoints=websecure"
      - "traefik.http.routers.prowlarr.tls=true"
      - "traefik.http.routers.prowlarr.service=prowlarr"
      # HTTP redirect router
      - "traefik.http.routers.prowlarr-http.rule=Host(`${PROWLARR_DOMAIN:-prowlarr.lan}`)"
      - "traefik.http.routers.prowlarr-http.entrypoints=web"
      - "traefik.http.routers.prowlarr-http.middlewares=redirect-to-https"
      - "traefik.http.routers.prowlarr-http.service=prowlarr"
      # Service definition
      - "traefik.http.services.prowlarr.loadbalancer.server.port=9696"

  bazarr:
    image: linuxserver/bazarr:latest
    container_name: bazarr
    restart: unless-stopped
    # Removed: ports - Access via Traefik at http://bazarr.lan
    volumes:
      - ./bazarr/config:/config
      - ${MOVIES_PATH:-/mnt/external/movies}:/movies
      - ${TV_PATH:-/mnt/external/tv}:/tv
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M
    networks:
      - media-network      # Keep for backward compatibility (Iteration 1)
      - backend-net       # Business logic layer
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6767/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "traefik.enable=true"
      # HTTPS router
      - "traefik.http.routers.bazarr.rule=Host(`${BAZARR_DOMAIN:-bazarr.lan}`)"
      - "traefik.http.routers.bazarr.entrypoints=websecure"
      - "traefik.http.routers.bazarr.tls=true"
      - "traefik.http.routers.bazarr.service=bazarr"
      # HTTP redirect router
      - "traefik.http.routers.bazarr-http.rule=Host(`${BAZARR_DOMAIN:-bazarr.lan}`)"
      - "traefik.http.routers.bazarr-http.entrypoints=web"
      - "traefik.http.routers.bazarr-http.middlewares=redirect-to-https"
      - "traefik.http.routers.bazarr-http.service=bazarr"
      # Service definition
      - "traefik.http.services.bazarr.loadbalancer.server.port=6767"
